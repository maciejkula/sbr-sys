language: rust
rust:
  - stable

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y gfortran

script:
  - cargo test
  - make cbindgen
  - git diff-index --quiet --cached HEAD --
